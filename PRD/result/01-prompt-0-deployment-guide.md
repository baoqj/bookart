# Prompt 0 部署指南

**功能模块**: 全站信息架构与路由骨架
**开发分支**: `feature/prompt-0-skeleton`
**部署日期**: 2026-01-20
**状态**: ✅ 已推送到 GitHub，等待 Vercel Preview 验证

---

## 一、Git 提交信息

### 提交记录
- **Commit Hash**: `0c8f093`
- **提交信息**: `feat: 完成 Prompt 0 - 全站信息架构与路由骨架`
- **分支**: `feature/prompt-0-skeleton`
- **远程地址**: `https://github.com/baoqj/bookart.git`

### 文件统计
- **总文件变更**: 217 个文件
- **新增行数**: 148,328 行
- **删除行数**: 5 行

---

## 二、Vercel 部署配置

### 2.1 首次配置步骤

1. **登录 Vercel**
   - 访问 https://vercel.com
   - 使用 GitHub 账户登录

2. **导入项目**
   - 点击 "Add New..." → "Project"
   - 选择 GitHub 仓库：`baoqj/bookart`
   - 点击 "Import"

3. **配置项目设置**

   **Framework Preset**: Next.js

   **Root Directory**: `Code`（重要：不是项目根目录）

   **Build Command**:
   ```bash
   pnpm build
   ```

   **Output Directory**: `.next`（默认）

   **Install Command**:
   ```bash
   pnpm install
   ```

   **Development Command**:
   ```bash
   pnpm dev
   ```

4. **环境变量**（本阶段无需配置）
   - 当前使用 Mock 数据，无需配置环境变量
   - 后续接入真实 API 时再配置

5. **部署设置**
   - **Production Branch**: `main`
   - **Preview Branches**: `feature/*`, `develop`
   - **Auto Deploy**: ✅ 启用

### 2.2 Vercel 配置文件

在项目根目录创建 `vercel.json`：

```json
{
  "buildCommand": "cd Code && pnpm build",
  "devCommand": "cd Code && pnpm dev",
  "installCommand": "cd Code && pnpm install",
  "framework": "nextjs",
  "outputDirectory": "Code/.next"
}
```

---

## 三、自动部署工作流

### 3.1 功能分支部署流程

1. **创建功能分支**
   ```bash
   git checkout -b feature/功能名称
   ```

2. **开发并提交代码**
   ```bash
   git add .
   git commit -m "feat: 功能描述"
   ```

3. **推送到远程**
   ```bash
   git push -u origin feature/功能名称
   ```

4. **自动触发 Preview 部署**
   - Vercel 自动检测新分支推送
   - 触发构建流程
   - 生成唯一的 Preview URL
   - 在 GitHub PR 中显示部署状态

5. **验收测试**
   - 访问 Preview URL 进行功能测试
   - 确认功能正常
   - 记录测试结果

6. **合并到 main**
   ```bash
   git checkout main
   git merge feature/功能名称
   git push origin main
   ```

7. **自动触发 Production 部署**
   - Vercel 检测 main 分支更新
   - 触发生产环境构建
   - 部署到生产域名

### 3.2 部署状态检查

**GitHub 集成**:
- 每次 push 后，GitHub 会显示 Vercel 部署状态
- 成功：✅ 绿色勾选
- 失败：❌ 红色叉号
- 进行中：🟡 黄色圆点

**Vercel Dashboard**:
- 访问 https://vercel.com/dashboard
- 查看所有部署记录
- 查看构建日志
- 访问 Preview URL

---

## 四、当前部署状态

### 4.1 GitHub 状态

- **仓库**: https://github.com/baoqj/bookart.git
- **分支**: `feature/prompt-0-skeleton`
- **推送状态**: ✅ 成功
- **提交**: `0c8f093`

### 4.2 Vercel 状态

- **项目配置**: ⏳ 待配置
- **Preview 部署**: ⏳ 待触发
- **Production 部署**: ⏳ 待合并

---

## 五、验收清单

### 5.1 Preview 环境验收

在 Vercel Preview URL 上测试以下内容：

#### 页面访问测试
- [ ] 根页面 (`/`) - 营销落地页
- [ ] 登录页面 (`/auth/sign-in`)
- [ ] 注册页面 (`/auth/sign-up`)
- [ ] 忘记密码 (`/auth/forgot`)
- [ ] 项目列表 (`/app/projects`)
- [ ] 创建项目 (`/app/projects/new`)
- [ ] 项目详情 (`/app/projects/test-id`)
- [ ] 书稿管理 (`/app/projects/test-id/manuscript`)
- [ ] 插图生成 (`/app/projects/test-id/illustrations`)
- [ ] 积分管理 (`/app/credits`)
- [ ] 账户设置 (`/app/account`)
- [ ] 管理员面板 (`/app/admin`)

#### 功能测试
- [ ] 响应式设计（移动端、平板、桌面）
- [ ] 主题切换（明亮/暗黑）
- [ ] 导航功能（侧边栏、面包屑）
- [ ] 页面加载速度
- [ ] 无控制台错误

#### 性能测试
- [ ] 首次加载时间 < 3 秒
- [ ] 页面切换流畅
- [ ] 无明显卡顿

### 5.2 验收通过标准

**必须满足以下所有条件**:
1. ✅ 所有页面可访问
2. ✅ 无 404 错误
3. ✅ 无控制台报错
4. ✅ 响应式设计正常
5. ✅ 主题切换正常
6. ✅ 导航功能正常

**验收通过后**:
- 创建 Pull Request
- 请求 Code Review（如有团队成员）
- 合并到 main 分支
- 触发生产环境部署

---

## 六、下一步操作

### 6.1 立即执行

1. **配置 Vercel 项目**
   - 登录 Vercel
   - 导入 GitHub 仓库
   - 配置 Root Directory 为 `Code`
   - 触发首次部署

2. **验证 Preview 环境**
   - 访问 Vercel 生成的 Preview URL
   - 按照验收清单逐项测试
   - 记录测试结果

3. **处理部署问题**（如有）
   - 查看 Vercel 构建日志
   - 修复错误
   - 重新推送代码
   - 再次验证

### 6.2 验收通过后

1. **创建 Pull Request**
   - 在 GitHub 创建 PR
   - 标题：`feat: Prompt 0 - 全站信息架构与路由骨架`
   - 描述：链接到测试报告

2. **合并到 main**
   ```bash
   git checkout main
   git merge feature/prompt-0-skeleton
   git push origin main
   ```

3. **验证生产环境**
   - 等待 Production 部署完成
   - 访问生产域名
   - 确认功能正常

4. **更新文档**
   - 在测试报告中添加 Preview URL
   - 记录生产环境 URL
   - 标记阶段完成

---

## 七、常见问题

### Q1: 构建失败怎么办？

**查看构建日志**:
1. 访问 Vercel Dashboard
2. 点击失败的部署
3. 查看 "Build Logs"
4. 定位错误信息

**常见问题**:
- 依赖安装失败 → 检查 `package.json`
- TypeScript 错误 → 运行 `pnpm build` 本地复现
- 环境变量缺失 → 在 Vercel 配置环境变量

### Q2: Preview URL 无法访问？

**检查项**:
1. 确认部署状态为 "Ready"
2. 检查 URL 是否正确
3. 清除浏览器缓存
4. 尝试无痕模式访问

### Q3: 如何回滚部署？

**在 Vercel Dashboard**:
1. 进入项目
2. 点击 "Deployments"
3. 找到上一个成功的部署
4. 点击 "..." → "Promote to Production"

### Q4: 如何添加自定义域名？

**步骤**:
1. 在 Vercel 项目设置中
2. 点击 "Domains"
3. 添加域名
4. 按照指引配置 DNS
5. 等待验证通过

---

## 八、附录

### 8.1 相关链接

- **GitHub 仓库**: https://github.com/baoqj/bookart
- **Vercel Dashboard**: https://vercel.com/dashboard
- **Next.js 文档**: https://nextjs.org/docs
- **测试报告**: `PRD/result/01-prompt-0-skeleton-test-report.md`

### 8.2 联系方式

**问题反馈**:
- GitHub Issues: https://github.com/baoqj/bookart/issues
- Email: （待补充）

---

**文档版本**: v1.0
**最后更新**: 2026-01-20 17:10
**状态**: 等待 Vercel 配置和 Preview 验证
